Upload files to this by posting them to the /upload endpoint

The file will then be listed here using the name it was posted with, e.g.
curl -k -T myfile.txt mirror-webserver-default.mycluster.com/upload/myfile.txt
